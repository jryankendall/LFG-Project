console.log("Test js loaded.");

/* function testRegisterUser(cb) {
    $.ajax("/api/user/dev/test/", {
    })
}

function testFindUser(cb) {
    $.ajax("/api/user/dev/test/", {
        type: "GET"
    })
    .then( (result, err) => {
        return cb(result);
    })
}

$("body").on("click", "#get-user-test", (event) => {
    event.preventDefault();
    console.log("You clicked this: " + event);
    

    testFindUser(console.log);
}) */